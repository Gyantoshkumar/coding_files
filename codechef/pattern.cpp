#include<bits/stdc++.h> 
using namespace std;
#define ll long long



#define pb push_back
#define f first
#define s second
#define mp make_pair

typedef vector < ll > vi;
typedef vector<vi> vvi;

int main()
{
    int i=0,j,k;
    int  n;
    cin>>n;
    vector<vector<ll> > v(n,vector<ll> (n));

    v[n-1][n-1]=(n*n);
    int sum=n*n;

    int k=0;
     while(k<sum)
     {
       k++;
       

     }
    return 0;
    
}